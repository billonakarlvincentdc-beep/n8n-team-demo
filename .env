# Use localhost when the demo runs on your machine (npm run dev) and n8n is on the same machine.
# Use host.docker.internal when the demo runs in Docker and n8n is on your host.
local_webhook_url=http://localhost:5678/webhook-test/protocol-demo
